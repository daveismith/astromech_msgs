std_msgs/Header   header    # stamp in UTC
uint8             level     # the sound level now
bool              level_set # false: the build's default, nothing has set it
LedDisplayState[] displays
LedSurfaceState[] surfaces
